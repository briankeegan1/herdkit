# tests/lever-reachability-exempt.tsv — the opt-out list for the INERT-LEVER guard (HERD-556,
# scripts/herd/lever-reachability-lint.sh).
#
# Format: <CONFIG_KEY><TAB><reason>. Blank lines and `#` comments are ignored. The reason is
# MANDATORY — a bare row reds as EXEMPT-MALFORMED rather than silently laundering a finding, because
# a row here is a DECISION on the record, not a silencer.
#
# A row is also RED once it goes STALE: if the key stops being a finding (its consumer chain became
# reachable, or the key was removed), the guard reports STALE-EXEMPT and the row must go. An
# exemption list that outlives the thing it excused is how a ratchet quietly stops ratcheting.
#
# TWO KINDS OF ROW, and the difference matters:
#
#   INTENTIONAL — a lever that is unreachable under the shipped engine ON PURPOSE (a bash-only knob
#   kept for a consuming project, a documented latent lever). The reason says why a future auditor
#   should NOT re-wire it. There are none yet.
#
#   BASELINE (HERD-556) — a lever that was ALREADY inert when this guard first ran, on 2026-08-05.
#   These are real findings, not exemptions: each is filed for the coordinator via `herd note` and is
#   deliberately NOT fixed in the guard's own PR (fixing eight levers blind, in the change that first
#   made them visible, is how a lint PR turns into an outage). They are listed so the guard ships
#   GREEN and works as a NO-NEW-INERT-LEVER ratchet from day one; the ADVISORY line keeps the count
#   visible ("N exempt (M silencing a finding)") so the debt cannot fade into the background. Remove
#   a row when its item lands — the STALE-EXEMPT rule above will insist.
#
# Every BASELINE row below is the same incident: the P5b engine port (HERD-306) moved gate dispatch
# into pysrc/herd/live_runtime.py and the bash gate pass lost its caller, so the levers whose only
# consumers hang off `_healthcheck_gate` / `_review_gate_step` / `_stale_dup_gate_step` read their
# configured value and change nothing. `GATE_DISPATCH` is the one the tree already admits to in
# prose: pysrc/herd/live_runtime.py:402 calls it "a lever the Python port never" implemented.

GATE_DISPATCH	BASELINE (HERD-556, 2026-08-05): only consumer is _gate_dispatch_mode (agent-watch.sh), reachable solely via _predispatch_review_if_parallel, which the Python core replaced at P5b. pysrc/herd/live_runtime.py:402 documents the gap in prose. Filed via herd note; not fixed here.
DELTA_REVIEW	BASELINE (HERD-556, 2026-08-05): only consumer is _delta_review_enabled (agent-watch.sh), reached only from the dead bash review pass; zero pysrc references. Named as a suspect on HERD-556 itself. Filed via herd note; not fixed here.
WATCH_CLAUDE_PROBE_TIMEOUT	BASELINE (HERD-556, 2026-08-05): only consumer is _claude_probe_secs (agent-watch.sh), reached only from the dead bash liveness-probe path; zero pysrc references. Filed via herd note; not fixed here.
ENV_SUSPECT_TIMEOUT	BASELINE (HERD-556, 2026-08-05): only consumer is _env_suspect_enabled (agent-watch.sh), reached only from the dead bash health-worker path; zero pysrc references. Filed via herd note; not fixed here.
HEALTH_LOAD_THRESHOLD	BASELINE (HERD-556, 2026-08-05): only consumer is _health_load_high (agent-watch.sh), reached only from the dead bash health-worker path; zero pysrc references. Filed via herd note; not fixed here.
REVIEW_MODEL_ESCALATED	BASELINE (HERD-556, 2026-08-05): only consumer is _review_gate_step (agent-watch.sh), the bash review gate the Python core replaced at P5b; zero pysrc references. Filed via herd note; not fixed here.
REVIEW_EVIDENCE_ESCALATE_ROUNDS	BASELINE (HERD-556, 2026-08-05): only consumer is _maybe_arm_review_escalation (agent-watch.sh), reached only from the dead bash review pass; zero pysrc references. Filed via herd note; not fixed here.
STALE_DUP_DETECT	BASELINE (HERD-556, 2026-08-05): consumers are stale_dup_enabled (stale-dup-gate.sh) and _stale_dup_gate_step (agent-watch.sh); the whole stale-dup gate hangs off _stale_dup_gate_step, which lost its caller at P5b. zero pysrc references. Filed via herd note; not fixed here.
