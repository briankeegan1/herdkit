# herdkit — backlog

> Single source of truth for planned herdkit work. The coordinator owns this file; the async
> scribe is the only automated writer. 🔜 planned · 🚧 in progress · ✅ shipped. herdkit is
> developed *using the herd* (workspace wB) — these items get built through its own lanes.

## Cross-repo orchestration (deferred — validate via a simulation FIRST, don't over-build; link registry foundation ✅ shipped PR #14)


## Enterprise / multi-user optionality (deferred — solo is the default today; bank the config seams now)

- 🔜 **Watcher flexibility for long-pending deps** — backoff polling; richer dep states (`open/in-review/in-progress/stalled/closed`); surface `stalled` + optional TTL so a slow enterprise PR never silently rots. A `blocked-on` is a status line, never a workspace freeze.
- 🔜 **Multi-user / team mode** — `WATCHER_SCOPE=mine|all` + ownership/assignee filter; auto-merge scoped to OWNED PRs only (never blind-merge teammates'), building on the required-checks gate (PR #5). `solo` default; `team` is a config flip.

## Workflow control & discoverability

> Build order: F1 (merge policy) first; capabilities manifest + template-sync guard as one PR; init detection + flow-preference interview as one PR; herd config last (leans on the manifest for key validation).

## Reliability / safety



## Efficiency / cost

> Spin-outs from the deferred [herdkit-vs-harness efficiency EPIC](#someday--deferred) (surfaced 2026-07-02). Token/cost instrumentation — `scripts/herd/cost.sh`, per-builder/review token accounting priced to USD, cost-per-merged-PR, `herd cost` command — is ✅ shipped (commit e2a0ae5); the instrumentation gate is lifted for all items below.


- ✅ **AUDIT (research pass): end-to-end flow redundancy, speed-ups, and gate necessity/correctness** — **Deliverable:** a findings doc (`docs/flow-audit.md`) + a RANKED list of concrete follow-up items, not a single code change; this is the umbrella under which the six spun-out efficiency levers below sit. **Why (surfaced 2026-07-02 to 2026-07-03):** the sha-cache fix (a held PR re-ran the full healthcheck every ~90s tick and manufactured its own transient code-error→flaky on PR #65) is likely one symptom of a broader pattern — the whole pipeline probably repeats work and runs gates that are redundant, unnecessary for some diffs, or sequenced sub-optimally. **Scope — every stage end-to-end:** (1) backlog/scribe — per-item git pulls, drainer spawn/teardown churn; (2) lane spawn — worktree + SHARE_LINKS symlinks, multi-kilobyte argv prompts; (3) build — isolated builders re-deriving context from files; (4) WATCHER TICK LOOP (`agent-watch.sh`) — how many `gh`/`herdr`/`git` subprocesses it forks per tick (latency + rate-limit + cost), what it recomputes every tick vs could cache, and whether the tick cadence itself is right; (5) HEALTHCHECK gate — serialization (`HEALTH_CONCURRENCY=1`: always necessary, or can worktree-isolated suites parallelize?), retry-before-red, and the now-added sha-cache; (6) REVIEW gate — per-PR Opus cost, whether trivial/docs/test-only diffs need it at all (ties to the risk-tiered-review item), and whether review + healthcheck overlap; (7) auto-refix rounds + wake verification; (8) merge re-verification; (9) reap — worktree remove, tab close, backlog reap, orphan sweep. **Look for four things at each stage:** (a) REDUNDANT work (repeated subprocess calls, re-reads, re-runs of unchanged inputs); (b) SPEED-UPS (caching, parallelization, better sequencing, cheaper models/tools); (c) UNNECESSARY gates — anything duplicating GitHub's own branch-protection/required-checks or adding latency without safety value; (d) gate ISSUES / correctness — races, false-reds, the manufactured-transient class, gates that can cache a wrong verdict. **Method:** token/cost instrumentation is ✅ shipped (commit e2a0ae5 — `herd cost`; per-builder/review accounting); add latency instrumentation as the audit proceeds, then map the flow, then rank findings; each significant finding becomes its own item; validate any flow change on the sandbox-consumer sim rig before shipping. **Composes with:** the six efficiency levers below (its children), the herdkit-vs-harness EPIC in `## Someday / Deferred` (shares the instrumentation precursor), and the sandbox-consumer rig (its safe test bed). **DELIVERABLE SHIPPED:** `docs/flow-audit.md` landed as PR #77 (commit 14f7e42); ranked follow-up items from that doc still need triaging into individual backlog items.


## Someday / Deferred

- 🔜 **Phase 4: onboard an external consumer** — `herd init` against a genuinely different repo (web server/library); the real abstraction test (§8).
- 🚧 **COMPETITIVE LANDSCAPE & PRIOR-ART RESEARCH (live web research — NOT from model training memory; findings PERISHABLE, refresh periodically; 🚧 worktree competitive-landscape — writing docs/fleet-landscape.md via live cited web research): AI-agent orchestration platforms, IDPs, monorepo tooling, and cross-project rollup tools vs. the fleet-coordinator vision** — Shared research precursor for both the Master/fleet-coordinator EPIC and the herdkit-vs-harness EPIC above; cross-reference those entries, do not duplicate here. **CRITICAL FRAMING:** the AI-agent-orchestration space moves monthly and any model's training knowledge (cutoff ~Jan 2026) is stale — this MUST be executed as a live `/deep-research` harness run against current web sources, with every finding TIMESTAMPED, every claim CITED, and the result written to `docs/fleet-landscape.md`; plan a periodic refresh cadence. **Research questions (the deliverable answers all six):** (1) AI-AGENT ORCHESTRATION PLATFORMS — current (post-Jan-2026) state of Factory.ai (Droids), Devin/Cognition, GitHub Copilot coding agent + any org/agents mission-control panel, Cursor background agents, Google Jules, OpenAI Codex cloud, Sourcegraph Amp, OpenHands: does ANY now offer a MASTER / cross-project coordinator that routes commands to per-project agents AND rolls up status across repos? Feature-by-feature comparison vs the fleet-coordinator vision. (2) INTERNAL DEVELOPER PLATFORMS / portals (Backstage, Port, Cortex, OpsLevel, Humanitec) — are any integrating autonomous AI coding agents into the catalog/relationship-graph/self-service-actions layer (an 'IDP for agents')? These are the closest PRIOR ART for 'tie all projects together under one control plane' — extract the design vocabulary to borrow: catalog, relationship/dependency graph, scorecards, self-service actions/templates, affected-detection, portfolio rollup. (3) MONOREPO / MULTI-REPO ORCHESTRATION (Nx + Nx Cloud, Turborepo/Vercel, Bazel, Pants, Sourcegraph Batch Changes, the meta tool, Renovate/Dependabot) — the proven 'project graph + run/affect across all' and 'fan one change to all repos' patterns; is anyone adding AI-agent fan-out on top? (4) CROSS-PROJECT ROLLUP / eng-intelligence (LinearB, Swarmia, Jellyfish, Jira Advanced Roadmaps) — the 'day summary across projects' analog; any adding agent command-dispatch vs pure reporting? (5) THE SPECIFIC FUSION — does ANYONE ship a persistent master coordinator over per-project AUTONOMOUS AI dev coordinators with NL command + cross-project digest, self-hosted / terminal-native? Who is closest, what is the exact GAP, and what is herdkit's defensible differentiation (self-hosted, transparent/hackable, model-flexible, PR/CI-native) — plus COMPETITIVE RISK: who could subsume it (GitHub, Factory) and how fast? (6) PRICING / positioning / adoption signals for the enterprise 'one person, many projects' persona. **Deliverable:** a cited, timestamped `docs/fleet-landscape.md` + a short positioning verdict (where herdkit is genuinely differentiated vs where the market already covers it), refreshed on a cadence. **Key reframe (surfaced 2026-07-02):** the 'tie multiple projects together under one control plane' pattern is MATURE and named — IDPs, monorepo project-graphs, GitOps fleets — herdkit's novelty is applying that proven control-plane pattern to AI-agent coordinators, not inventing the concept; the landscape doc should adopt this vocabulary precisely and locate herdkit's differentiation within it.
- 🔜 **EPIC (optional, enterprise-oriented): Master / fleet coordinator — a parent orchestration layer above the per-project coordinators** *(✅ P0 shipped PR #94 — project registry + herd fleet register/list/status/upgrade/reload; P1 digest, P2 inbox, P3 NL agent, P4 governance stay 🔜, keep EPIC open)* — **Vision (2026-07-02):** manage N herd projects (e.g. herdkit + northstar) from one meta-workspace — fan a single command out to ALL repos (e.g. `herd upgrade` into every repo at once), dispatch a task down to one or many projects, route natural-language commands ("send this to herdkit", "what is going on with northstar today?"), and get a cross-project daily digest of everything done across projects; the master knows the operator's full workflow and the relationships between projects; the operator can still hone in and work in one individual project's control room as needed. **Why it fits:** this is the clearest differentiator herdkit has over the raw Claude harness — persistent, multi-project, human-supervised across days; the harness is single-session/single-repo and architecturally cannot do this; cross-reference the herdkit-vs-harness EPIC — the master coordinator is a flagship instance of herdkit's only real moat (decoupling from session lifetime + operator presence, now across many projects). **Architecture sketch:** a coordinator-of-coordinators in its own meta-workspace over (1) a PROJECT REGISTRY mapping each project's `WORKSPACE_NAME` → `path/repo/.herd/config` (explicit `~/.herd/fleet` list or auto-discovered by scanning roots for `.herd/config`; `herdr workspace list` already enumerates live ones); (2) TWO COMMAND CHANNELS — a DETERMINISTIC fan-out (no LLM) that loops the registry running existing per-project herd commands (`update/reload/status`) and returns a per-project rollup table (the upgrade guard already refuses on dirty tree / mid-flight builders, making bulk-upgrade safe), and an NL ROUTING channel that resolves target label(s) and dispatches a task into each target project's coordinator (via `herdr pane run`, the same nudge mechanism) or a per-project inbox; (3) JOURNAL AGGREGATION for status/digest — every project already writes `.herd/journal.jsonl` (`herd why/log`), so the master rolls those up across projects. **Additional features:** (a) CROSS-PROJECT ATTENTION INBOX — one view of what needs the human right now across all projects (blocked PRs, human-verify holds, conflicts, failed gates); (b) DAILY DIGEST / STANDUP built from aggregated journals (shipped/blocked/in-flight per repo); (c) GLOBAL CONCURRENCY + TOKEN GOVERNANCE — cap total builders/reviews across all projects, since the Claude account usage limit is account-wide (observed 2026-07-02: herdkit + northstar competing for one quota caused a mid-task limit hit); this is the right place to prevent limit-hits and ties into the efficiency EPIC's cost instrumentation and the limit-auto-resume item; (d) POLICY PROPAGATION — set `MERGE_POLICY` / model tier / `TOKEN_MODE` across the whole fleet at once; (e) DEPENDENCY-AWARE UPGRADE FAN-OUT — when herdkit ships a breaking engine change, auto-queue the upgrade in consumer repos (ties into `.herd/deps` + the herd-upgrade versioned-migrations item); (f) WATCHER HEARTBEAT MONITOR — per-project "is each watcher alive", now attributable via the `herd-watch-<workspace>` argv0 marker shipped in PR #65; (g) register/deregister a project with the fleet. **Dependencies:** HARD dep on the workspace-scoping isolation hardened in argv0 attribution PR #65 and the launch-binding-guard in-flight item — safe fan-out and per-project liveness attribution rest on it; builds on the existing cross-repo link registry (`.herd/links`) + `.herd/deps` foundation; SOFT dep on the sandbox-consumer sim rig as the safe multi-project test bed (fan-out to N fake projects, assert isolation + rollup) — not required to design it, and P0 is testable carefully against real projects. **Phasing:** P0 ✅ shipped PR #94 — deterministic `herd fleet register/list/status/upgrade/reload` + project registry; P1 cross-project digest/standup; P2 attention inbox; P3 the NL master-coordinator agent; P4 relationship graph + policy propagation + global concurrency/token governance. **Relevant existing primitives:** `.herd/links`, `.herd/deps`, `herd report --to`, `journal.sh`, `herdr workspace list`, per-project `WORKSPACE_NAME` resolution. **Competitive landscape:** see the shared _Competitive Landscape & Prior-Art Research_ item below — run a live, dated `/deep-research` pass (NOT answered from model training memory) before committing to architecture; the IDP and monorepo project-graph vocabulary maps directly to the fleet-coordinator design and should be borrowed explicitly.

## Recently shipped
- ✅ **Positioning thesis — herdkit vs raw Claude harness (docs/positioning-thesis.md)** *(PR #95)*
- ✅ **Auto-refix / direct hand-offs bypass backlog reconciliation — option (a): post-merge auto-reconcile hook in agent-watch.sh keyed by PR#+worktree-slug** *(PR #93)*
- ✅ **Fleet coordinator P0 — deterministic multi-project fan-out (herd fleet register/list/status/upgrade/reload + project registry)** *(PR #94 · commit be353ee)*
- ✅ **Sandbox consumer — a dummy repo + herdr workspace for live workflow simulation, especially pane/tab behavior** *(PR #92)*
- ✅ **Evidence-based model escalation — part (a): eco-leaning STARTER defaults for new installs** *(PR #91)*
- ✅ **Dispatch vs. dependency intent** *(PR #90)* — `.herd/deps` schema + `herd depend/deps list/rm`; fire-and-forget `--to B` vs tracked `--dep` with `blocked-on` + reclassify/remove via `herd deps rm|demote`

- ✅ **Coordinator 'Workflow settings' menu entry** *(PR #88)*
- ✅ **Flow-preference interview + draft-PR flow** *(PR #89)* — bounded first increment; `LOCAL_REVIEW=pre-pr` wiring is an open follow-up
- ✅ **`herd upgrade` versioned migrations** *(PR #87)*
- ✅ **`herd config` + coordinator Workflow settings mode** *(PR #85 CLI · PR #88 menu entry)* — `herd config list|get|set` with key validation + restart/re-render awareness; coordinator-menu 'Workflow settings' entry ✅ shipped in PR #88 — full feature complete
