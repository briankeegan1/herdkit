# herdkit — backlog

> Single source of truth for planned herdkit work. The coordinator owns this file; the async
> scribe is the only automated writer. 🔜 planned · 🚧 in progress · ✅ shipped. herdkit is
> developed *using the herd* (workspace wB) — these items get built through its own lanes.

## Cross-repo orchestration (deferred — validate via a simulation FIRST, don't over-build; link registry foundation ✅ shipped PR #14)

- 🔜 **`herd upgrade` versioned migrations** — `migrations/vN→vM.sh` transform a consumer's config/hooks to a new engine contract, so breaking changes are inherited without clobbering custom setup. *(Gap 4 in `docs/gap-report-cross-repo-loop.md`.)*

## Enterprise / multi-user optionality (deferred — solo is the default today; bank the config seams now)

- 🔜 **Dispatch vs. dependency intent** — `herd report --to B` (fire-and-forget issue, default) vs `--to B --dep` / `herd depend B#id` (records `blocked-on` + watched). Reclassify/remove via `herd deps rm|demote` — a dep is editable data, never stuck. *(Gap 3 in `docs/gap-report-cross-repo-loop.md`: `.herd/deps` schema + `herd depend/deps list/rm`; depends on the link registry above for `--to <project>`.)*
- 🔜 **Watcher flexibility for long-pending deps** — backoff polling; richer dep states (`open/in-review/in-progress/stalled/closed`); surface `stalled` + optional TTL so a slow enterprise PR never silently rots. A `blocked-on` is a status line, never a workspace freeze.
- 🔜 **Configurable watcher views** — lenses `mine | all | deps | review-queue` + filters (author/assignee/label/status); default in `.herd/config`.
- 🔜 **Multi-user / team mode** — `WATCHER_SCOPE=mine|all` + ownership/assignee filter; auto-merge scoped to OWNED PRs only (never blind-merge teammates'), building on the required-checks gate (PR #5). `solo` default; `team` is a config flip.

## Workflow control & discoverability

> Build order: F1 (merge policy) first; capabilities manifest + template-sync guard as one PR; init detection + flow-preference interview as one PR; herd config last (leans on the manifest for key validation).

- 🔜 **Init-time GitHub detection** — `cmd_init` (bin/herd:104-284) makes zero `gh api` calls today; add a detection pass: branch protection on the default branch, required-review count, required checks, CODEOWNERS, allowed merge methods; show findings and derive safe defaults (required reviewers present ⇒ default `MERGE_POLICY=approve`, never `auto`); degrade gracefully without remote/gh.
- 🔜 **Flow-preference interview + draft-PR flow** — New `herd init` questions → config keys: `PR_FLOW=direct|draft` (draft: lanes instruct builders `gh pr create --draft`; watcher already holds `DRAFT`, agent-watch.sh:157), `PR_READY_WHEN=builder|coordinator|human`, `LOCAL_REVIEW=none|pre-pr` (run the review gate in the worktree BEFORE the PR is public vs today's post-PR review), `MERGE_METHOD`, `DELETE_BRANCH_ON_MERGE`; thread prefs into the lane rules text (herd-quick.sh:58-61, herd-feature.sh:50-53).
- 🔜 **`herd config` + coordinator Workflow settings mode** — `herd config list|get|set` with key validation, aware of what each change requires: watcher keys ⇒ restart watcher (pid file `.watcher-<slug>.pid`), coordinator-facing keys ⇒ re-render skill (render step at bin/herd:277-278); coordinator menu gains a 'Workflow settings' entry to view/change any workflow pref anytime and relaunch affected pieces — nothing is init-only.
- 🔜 **Backlog-reconcile step in cutover / extraction / rename PRs** *(issue #17)* — when a PR moves or renames the things that backlog items point at (file paths, function names, section headers, worktree names), affected entries dangle silently after the PR lands. The coordinator / scribe should run a reconcile pass as part of landing such PRs: diff the rename surface, identify backlog entries that reference moved things, and update or flag them so the backlog stays coherent with the live codebase.
- 🔜 **Analyst seat launch config must include an explicit context-persistence tenet** *(issue #19)* — the playground analyst seat's herdkit-hosted launch prompt has no cross-session context-persistence instruction, so the seat can silently go stateless between sessions: the only instance of that behavior today survives as a pruneable memory entry in one consumer project (northstar), not as a durable directive in the seat config. Fix: add an explicit tenet to the analyst seat's launch prompt / config requiring it to actively build and update the user's project and domain context in the local project memory store on every session — context stays local and gitignored per the existing memory contract; this is a prompt/config-only change with no engine wiring.
- 🚧 **`herd update` — one-command engine update + upgrade + reload, surfaced in the coordinator skill** *(worktree herd-update)* — Updating a consumer project today is a manual 3-step (git pull in the herdkit clone → herd upgrade to re-render the skill preserving .herd/config → herd reload to bounce the watcher onto new code), which nobody remembers; the first external consumer needed it spelled out by hand (issue #31 fix). Build: (1) a `herd update` subcommand (or `herd upgrade --pull --reload` lever) that refuses if the HERDKIT_HOME clone is dirty, git pull --ff-only's it, shows a compact delta (`git log --oneline old..new`), runs the existing cmd_upgrade re-render (respecting the future versioned-migrations item in Cross-repo orchestration), then invokes cmd_reload so the watcher + panes run the new engine — with a warning/hold if builders are mid-flight (their lanes reference engine scripts live); (2) the rendered coordinator skill gains an 'Update engine' capability entry so the coordinator can offer/run it on request (e.g. when a fix lands upstream, the answer to 'how do I get it?' becomes: ask your coordinator to run herd update); (3) capabilities.tsv row + hermetic tests (stub git/herdr). Depends on: herd reload v2 (worktree reload-full-workspace) — sequence AFTER it merges to avoid bin/herd conflicts.
- 🔜 **Parallel review dispatch — stop head-of-line blocking the merge queue** *(next — builder spawns after PR #38 tab-teardown merges; both edit agent-watch.sh)* — the watcher runs the review gate SYNCHRONOUSLY in its poll loop (agent-watch.sh:487 captures herd-review.sh's verdict in a blocking command substitution), so exactly one review runs at a time and a long Opus review (~7 min observed) head-of-line-blocks every other PR's review AND all merges for the rest of that tick cycle; observed 2026-07-02 with two ready PRs (reload-full-workspace under review while tab-teardown waited idle). Build: (1) dispatch herd-review.sh per unreviewed PR in the BACKGROUND, writing the verdict to a per-PR result file ($WORKTREES_DIR/.review-inflight-<pr>-<sha> marker while running so a PR is never double-dispatched — mirror the sha-keyed review-once semantics), and have the watcher collect finished verdicts on subsequent ticks instead of blocking; (2) bound concurrency with a config key (REVIEW_CONCURRENCY, default 2) since review model cost is the real limit; (3) keep merges flowing: a PR whose verdict is already cached PASS merges immediately even while other reviews run; (4) the streaming review panes already exist per-PR so parallel reviews each get their own tab; (5) crash-safety: a stale inflight marker (no live process) is reaped and re-dispatched, and a severed review still writes INFRA-FAIL per the 'infra death vs refused verdict' item — build on top of that; hermetic tests stub herd-review with controllable durations to assert no double-dispatch, bounded concurrency, and verdict collection across ticks.
- 🔜 **Auto-refix — watcher bounces BLOCK reviews straight to the builder** *(sequence AFTER Parallel review dispatch — both touch agent-watch.sh)* — the dominant manual chore across PRs #28, #30, #32, #38: a builder opens its PR, goes idle, a BLOCK review lands, and nothing reacts until a human notices and re-tasks the builder by hand. Build: (1) when the watcher records a BLOCK verdict for PR <n> (slug S), it automatically delivers a re-task prompt to S's builder agent pane containing the full review comment + fix instructions — CRITICAL delivery detail: find the AGENT pane (herdr pane/agent list entry whose label is the agent name and agent_status is idle), NOT the tab's root shell pane (text sent there vanishes and the agent never wakes); verify agent_status flips to working within ~15s, retry once, else surface 'needs you · auto-refix failed' on the console row; (2) sha-keyed refix-once semantics (mirror review-once): one auto-bounce per BLOCK verdict per sha so a builder that pushes a bad fix gets exactly one new bounce for the new sha's BLOCK — no infinite loops; cap total auto-refix rounds per PR (config, default 3) then escalate to needs-you; (3) config lever `REVIEW_AUTOFIX=true|false` (default false for consumers; set to true in herdkit's own `.herd/config` as part of this PR — dogfood on); (4) console rows show 'refixing (round k/3)' while a bounce is in flight; (5) capabilities.tsv row + coordinator template mention so rendered coordinators know the lever; (6) hermetic tests: stub herdr pane/agent list+run, assert correct-pane targeting, refix-once per sha, round cap, and failure escalation path.

## Reliability / safety

- 🔜 **Pre-trust fix (PR #22) is ineffective — builders still die on the folder-trust prompt** — new-feature.sh:43-49 writes an empty {} to `<worktree>/.claude/settings.json` claiming Claude Code treats any project-level settings file as a trust marker; that premise is wrong (folder trust is recorded in `~/.claude.json` keyed by project path), so freshly-spawned builders can still hit the interactive trust prompt and die/vanish with zero commits — observed with worktree watch-pr-display: agent pane died at startup leaving an empty shell pane, watcher correctly flagged 'idle · no PR'. Real fix candidates, to be verified against how Claude Code actually records trust: seed the project entry in `~/.claude.json` (e.g. `hasTrustDialogAccepted`) before launch, or have the launcher detect the trust prompt in the pane and answer it, or find a supported non-interactive trust flag. The stalled-builder detection half of PR #22 works and stays.
- 🔜 **Stall detector: check worktree mtimes before flagging** — `agent-watch.sh:383-392` flags any working builder with zero branch commits after 5 minutes as 'stalled? · check pane', but builders normally commit ONCE at the end right before `gh pr create`, so every normal build >5 min shows a false stall warning (observed 2026-07-01: herd-reload and workspace-pin both flagged while actively editing — one had file edits 0 minutes old). Refine: before flagging, check the newest mtime among the worktree's dirty/untracked files (`git status --porcelain` paths); recent edits (< ~5 min) ⇒ show 'building (uncommitted changes)' instead of the stall warning; only flag when the tree is clean AND commitless AND old, or dirty but untouched for >N min. Keep the warning for the genuinely-dead case (agent status not 'working' already shows 'idle · no PR').
- 🔜 **Mandatory dependency checks in install.sh + a `herd doctor` preflight** — the first external-consumer install (Windows) produced a partially-broken control room with no error pointing at the cause; a missing load-bearing dependency is the prime suspect, and nothing checks them today: `install.sh` verifies zero dependencies, `cmd_init` only soft-probes `claude` (bin/herd:149) and `gh` (bin/herd:189), and `herd-preflight.sh` checks ONLY herdr CLI presence + JSON contract. Notably `python3` — used by nearly every lane script to parse herdr JSON (`python3 -c` in coordinator.sh, scribe.sh, herd-quick.sh, herd-feature.sh, herd-review.sh, research.sh, herd-resolve.sh) — is never checked anywhere and is a classic Windows gap (often only `python` exists). **Confirmed root cause (issue #31, Windows/bootyslime):** beyond mere presence, python3 on Windows defaults stdout/stderr to the cp1252 codepage, and the herd scripts print strings with emoji/non-ASCII (🐑/🛰/✍️ pane labels) through `python3 -c`, causing a `UnicodeEncodeError` that kills the subcall; `coordinator.sh`'s `set -euo pipefail` + `>/dev/null` silently swallows the error and the layout truncates to a lone coordinator pane — this is the actual failure mode. Fix: `export PYTHONUTF8=1` (or `PYTHONIOENCODING=utf-8`) once in `scripts/herd/herd-config.sh` so every `python3` subcall is UTF-8-safe regardless of platform codepage; keep non-ASCII out of machine-parsed output paths. The doctor must check not just python3 presence but also its effective codepage/encoding on Windows. Build: (1) extend `herd_preflight` (scripts/herd/herd-preflight.sh) into a full dependency doctor — HARD-require `git`, `gh` (+ `gh auth status`), `claude`, `python3`, and `herdr`; report every missing dep in one pass with per-platform install hints, not fail-on-first; (2) run it from `install.sh` and at the top of `cmd_init` so a broken environment is caught before any config is written; (3) optional deps stay soft with explicit fallback notes: `glow` (backlog-view.sh:63-64 already degrades), `shellcheck`/`bats` (healthcheck already degrades); (4) expose as `herd doctor` so a user with a weird setup can self-diagnose anytime. Related: the '`herd init` must end in a verified 3-pane control room' item — doctor is the first gate of that verified launch.
- 🔜 **Healthcheck must gate / flag interaction tests for app-facing PRs** *(issue #15)* — the consumer healthcheck profile today runs only a render smoke (e.g. `st.testing.v1.AppTest.from_file`), which is blind to broken interactivity: a persistent quick-set radio silently overrode a slider every re-run in northstar PR #48 while the smoke passed. Engine-side ask: the lane rules / healthcheck contract should let a project declare interaction-test expectations (e.g. Streamlit `AppTest`: drive the widget, then assert the dependent output changes) and at minimum flag their *absence* for PRs that touch the app surface — a healthcheck that cannot observe widget → output causality should not silently green-light an app-facing PR.
- 🔜 **Review gate must distinguish infra death from a refused verdict** — observed on PR #32 (2026-07-02): the watcher's review capture (agent-watch.sh:487 command substitution → herd-review.sh → claude -p) was killed mid-review (prime suspect: a builder verifying kill logic whose pgrep -f agent-watch.sh sweep matched the watcher's own review fork), the capture returned EMPTY, and the gate posted 'no parseable verdict (defaulting to BLOCK)' and CACHED that BLOCK against the sha — so a purely infrastructural death became a sticky verdict requiring manual ledger surgery, while the orphaned reviewer finished with no pipe to report through (no PR comment, no verdict recorded). Build: (1) treat an EMPTY capture (vs an explicit 'REVIEW: BLOCK — reason') as INFRA-FAIL: do not cache, retry next poll with a retry cap, and say 'review infrastructure failed' not 'reviewer blocked'; (2) herd-review.sh should trap SIGTERM/SIGPIPE and flush 'REVIEW: INFRA-FAIL' so a killed run still reports; (3) make the reviewer chain resistant to stray-sweeps: distinctive process naming (e.g. exec -a herd-review-gate) so kill-by-pattern tools can exclude it, and lane rules must forbid builders from running kill/stop logic against the live workspace during verification (third live-infra interference tonight: killed the production watcher, leaked five strays, now severed an in-flight review).
- 🔜 **Watcher process must carry a per-workspace argv0 so cross-project sweeps cannot kill the wrong instance** — every project sharing one herdkit install runs the watcher via the identical script path (`bash <herdkit>/scripts/herd/agent-watch.sh`), so any kill-by-pattern — a builder's test sweep, a user's `pkill -f agent-watch.sh`, or a `cmd_reload` — hits EVERY project's watcher at once; observed 2026-07-02: northstar's watcher was found dead (no process, no lockfile) as apparent collateral from herdkit-side sweeps. Fix: `agent-watch.sh` should `exec -a herd-watch-<WORKSPACE_NAME>` itself at startup so the process appears in listings under a distinctive per-workspace name; `cmd_reload` and any `pgrep` guidance in docs/lane-rules should match `herd-watch-<WORKSPACE_NAME>` rather than the shared script path; this also makes orphan detection more reliable since a live watcher's name encodes its workspace.
- 🔜 **Sandbox consumer — a dummy repo + herdr workspace for live workflow simulation, especially pane/tab behavior** — every painful bug on 2026-07-02 was integration-level and VISUAL (review pane blank from `claude -p` buffering; reload's watcher pane sitting at a bare prompt while the process ran detached; spawns landing in whichever workspace had focus; stranded review/resolve tabs; cp1252 silently truncating the control room) — the hermetic test suite structurally cannot see any of these because it stubs herdr and never renders a pane. Build a live simulation rig: (1) a dedicated sandbox repo hosted on GitHub (e.g. `herdkit-sandbox`: a tiny real app + seeded BACKLOG.md) with a `herd sim init`-style fixture generator that deterministically resets it (fresh branch, clean worktrees dir, seeded issues) so every run starts identical — reuse the conventions from the shipped cross-repo loop simulation (PR #24, scripts/herd/sim); (2) a dedicated herdr workspace ('sandbox') where scenarios spin up a REAL control room so pane/tab experiments never touch real projects; assert placement/layout/labels/teardown via `herdr tab list` / `pane list` / `agent list` JSON at each checkpoint; (3) scripted end-to-end scenarios: init → control-room launch → spawn builder → PR → review gate → merge → teardown, with a STUB builder agent (deterministic tiny change, no model call) so runs are fast/cheap, plus an opt-in real-model smoke mode; (4) VISUAL confirmation: capture pane contents (herdr capture API if available, else launch a terminal window and take OS screenshots, e.g. macOS `screencapture` of the herdr window) at checkpoints; keep golden screenshots and have a vision-capable `claude -p` judge assert observable truths ('3 panes present', 'watch console painting rows', 'review pane streaming text') rather than pixel-diffing; (5) fault-injection scenarios reproducing 2026-07-02's bug classes: kill the watcher mid-review (expect INFRA retry not cached BLOCK), never-committed backlog file, `PYTHONIOENCODING=ascii` to simulate cp1252, missing optional deps (glow), duplicate-watcher race, focused-workspace misdirection — assert LOUD failures, never silent truncation; (6) wire as an opt-in heavy gate: engine PRs touching pane-creating scripts (coordinator.sh, lanes, herd-review.sh, agent-watch.sh, cmd_reload) should trigger a sandbox sim run before merge — like `HEALTHCHECK_HEAVY_GLOB` but for workflow surface; (7) each sim run ends with a machine-readable scorecard (scenario, checkpoints passed, screenshots dir) so the coordinator can review results and a human can eyeball the visual evidence.

## Someday / Deferred

- 🔜 **Phase 4: onboard an external consumer** — `herd init` against a genuinely different repo (web server/library); the real abstraction test (§8).
- 🔜 **Claude-plugin wrapper** — ship the skill as a Claude Code plugin while the CLI stays source of truth (§4).

## Recently shipped

- ✅ **Tab teardown must cover every tab the engine creates** *(PR #38)*
- ✅ **`herd reload` v2 — full workspace reload, coordinator-safe** *(PR #37)*
- ✅ **backlog-view crash on never-committed backlog** *(PR #36)*
- ✅ **`herd init` must end in a verified 3-pane control room** *(PR #34)*
- ✅ **`herd reload` — one-command workspace reload** *(PR #32)*
- ✅ **Review-gate visibility + human override** *(PR #33)*
- ✅ **Pin every herdr tab/agent spawn to the project workspace** *(PR #30)*
- ✅ **Capabilities manifest** *(PR #28)*
- ✅ **Herd-watch console: show PR # on in-flight rows** *(PR #29)*
- ✅ **Merge policy + approval queue** — MERGE_POLICY=auto|approve|observe, MERGE_METHOD, sha-keyed approvals ledger + herd-approve.sh. *(PR #26)*
